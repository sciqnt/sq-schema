# GENERATED by sciqnt/sciqnt scripts/publish_component.py — do not hand-edit during
# the transition; edit core/sq_schema in the monorepo and re-publish.
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sciqnt-schema"
version = "0.1.0"                  # stamped by the publishing bot (transition)
description = "sq-schema — the canonical cross-asset contract for sciqnt (point-in-time-correct schema + conformance + JSON-Schema artifact)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "sciqnt" }]
dependencies = [
    "pydantic>=2.5,<3",
]

[project.urls]
Homepage = "https://github.com/sciqnt/sq-schema"
Source = "https://github.com/sciqnt/sq-schema"

[tool.setuptools]
package-dir = { "" = "src" }
packages = ["sq_schema"]

[tool.setuptools.package-data]
sq_schema = ["contract.schema.json"]
